### Fixed

### Added

### Changed

***TODO***

- NCA now *requires* a version of Open ONI with a plugin which exposes APIs for
  loading batches, purging batches, and waiting for either of those two jobs to
  complete. This drastically simplifies the NCA process of managing batches,
  and eliminates some inconsistencies / problems associated with managing
  batches manually. It does, however, complicate using NCA standalone.
  - A new setting, `TBD`, can be used to fake a successful remote calls for all
    integrations. This is primarily meant for one-off use of NCA, as it
    requires keeping track of anything NCA thinks it's synced to ONI, and
    manually doing that syncing (***TODO***: batches for now, but anything
    else?)

### Removed

***DONE***

- All manual flagging of batch loading and purging has been removed, as have
  the instructions for these pieces. The instructions were a bit broken in any
  case, but now that we have an API to tell ONI to load and purge
  automatically, we've been able to ditch all the "do a manual thing" pieces of
  NCA instead of trying to fix the instructions on their own.

### Migration

